# 🔒 راهنمای ساخت نسخه محافظت‌شده

## مشکل شما چیست؟
شما می‌خواهید:
1. **کد برنامه قابل خواندن نباشد** - کسی نتونه سورس رو ببینه
2. **حجم خیلی کم باشد** - پوشه offline و soft چند گیگابایت هستند
3. **نصب آسان باشد** - موقع اجرا خودش فایل‌ها رو استخراج کنه

## راه‌حل

### روش 1: استفاده از اسکریپت آماده (ساده‌ترین)

```batch
BUILD_RELEASE.bat
```

این کار می‌کنه:
- Python رو به فایل EXE تبدیل می‌کنه (غیرقابل دیکامپایل)
- پوشه‌های بزرگ رو با 7z فشرده می‌کنه (۵۰-۷۰٪ کاهش حجم)
- یک پوشه `release` می‌سازه که آماده انتقاله

### روش 2: دستی

#### مرحله ۱: نصب نیازمندی‌ها
```bash
pip install nuitka ordered-set zstandard
```

#### مرحله ۲: اجرای اسکریپت
```bash
python build_release.py
```

## خروجی چیه؟

```
release/
├── OdooInstaller.bat      # فایل اجرایی اصلی
├── ui_server.exe          # سرور کامپایل شده (غیرقابل خواندن)
├── extract_files.bat      # استخراج‌کننده خودکار
├── offline.7z             # فایل‌های نصب فشرده (~500MB به جای 2GB)
├── soft.7z                # نرم‌افزارها فشرده (~100MB به جای 400MB)
├── web/                   # فایل‌های رابط کاربری
└── installer_config.json  # تنظیمات
```

## چطور کار می‌کنه؟

1. **کاربر فایل رو اجرا می‌کنه** → `OdooInstaller.bat`
2. **اگر فایل‌ها استخراج نشدن** → خودکار `offline.7z` و `soft.7z` رو باز می‌کنه
3. **سرور اجرا می‌شه** → مرورگر باز می‌شه

## مزایا

| قبل | بعد |
|-----|-----|
| فایل‌های Python قابل خواندن | فایل EXE کامپایل شده |
| ~3GB حجم | ~800MB فشرده |
| کدها قابل کپی | محافظت شده |

## نکات مهم

### ❌ نیازی نیست نگران باشید:
- **Nuitka** کد Python رو به C تبدیل می‌کنه و بعد به باینری
- این روش از PyInstaller امن‌تره چون کد واقعاً کامپایل می‌شه
- برگرداندن به Python اصلی تقریباً غیرممکنه

### ⚠️ محدودیت‌ها:
- فایل‌های HTML/CSS/JS همچنان قابل دیدن هستند (رابط کاربری)
- اما منطق اصلی برنامه (لایسنس، دیتابیس، API) محافظت شده

### 🔐 برای امنیت بیشتر:
1. `admin_config.py` رو قبل از ساخت ویرایش کنید (رمز ادمین)
2. `license_manager.py` رو چک کنید (کلید مخفی SECRET_KEY)

## سوالات متداول

**س: آیا کسی می‌تونه لایسنس رو دور بزنه؟**
ج: با کامپایل Nuitka، کد لایسنس داخل باینری هست و خواندنش خیلی سخته.

**س: چرا هنوز فایل‌های web قابل دیدن هستند؟**
ج: چون مرورگر باید بتونه HTML/CSS/JS رو بخونه. اما API و منطق سرور محافظت شده.

**س: اگر 7-Zip نصب نباشه چی؟**
ج: می‌تونید `7za.exe` (نسخه کوچک) رو کنار فایل‌ها بذارید.
