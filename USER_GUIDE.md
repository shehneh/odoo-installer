# 🚀 راهنمای کامل استفاده از Odoo Installer

## 📋 فهرست
1. [ساخت نسخه برای مشتری](#1-ساخت-نسخه-برای-مشتری)
2. [انتقال به سیستم مشتری](#2-انتقال-به-سیستم-مشتری)
3. [نصب روی سیستم مشتری](#3-نصب-روی-سیستم-مشتری)
4. [صدور لایسنس برای مشتری](#4-صدور-لایسنس-برای-مشتری)
5. [سوالات متداول](#5-سوالات-متداول)

---

## 1. ساخت نسخه برای مشتری

### مرحله ۱: نصب نیازمندی‌ها (یکبار)
```batch
pip install nuitka ordered-set zstandard
```
و نصب 7-Zip از https://7-zip.org

### مرحله ۲: ساخت نسخه
```batch
BUILD_RELEASE.bat
```

### مرحله ۳: خروجی
پوشه `release` ساخته می‌شه با این محتوا:
```
release/
├── OdooInstaller.bat      ← فایل اجرایی اصلی
├── ui_server.exe          ← سرور (کد محافظت شده)
├── extract_files.bat      ← استخراج‌کننده خودکار
├── offline.7z             ← Python, PostgreSQL, Git, NodeJS, wheels (فشرده)
├── soft.7z                ← VSCode, WinRAR, ... (فشرده)
├── web/                   ← رابط کاربری
│   ├── index.html
│   ├── license.html       ← صفحه فعال‌سازی لایسنس
│   ├── admin_login.html   ← ورود ادمین (برای شما)
│   └── ...
├── installer_config.json
└── start_ui.bat
```

---

## 2. انتقال به سیستم مشتری

### چه چیزی منتقل می‌شه؟
✅ **همه چیز لازم برای نصب آفلاین اودو:**
- Python 3.11
- PostgreSQL
- Git
- NodeJS  
- Visual C++ Redistributable
- wkhtmltopdf
- تمام پکیج‌های pip (wheels)
- VSCode و افزونه‌ها (اختیاری)

### روش انتقال:
1. **فلش/هارد**: کل پوشه `release` رو کپی کنید
2. **لینک دانلود**: آپلود کنید روی Google Drive / Dropbox
3. **CD/DVD**: اگر حجم جا شد

### حجم تقریبی:
| قبل از فشرده‌سازی | بعد از فشرده‌سازی |
|------------------|------------------|
| ~3 GB | ~800 MB - 1 GB |

---

## 3. نصب روی سیستم مشتری

### قدم ۱: اجرای برنامه
مشتری `OdooInstaller.bat` رو اجرا می‌کنه:
- اگر اولین بار باشه، فایل‌های `offline.7z` و `soft.7z` خودکار استخراج می‌شن
- مرورگر باز می‌شه: `http://127.0.0.1:5000`

### قدم ۲: صفحه لایسنس
اولین چیزی که می‌بینه صفحه فعال‌سازی لایسنسه:
```
🔐 فعال‌سازی لایسنس
━━━━━━━━━━━━━━━━━━━━
🖥️ شناسه سخت‌افزاری: 8e9666bd4cdbe30d
🔑 کلید لایسنس: [____________]
           [✓ فعال‌سازی]
```

### قدم ۳: دریافت شناسه سخت‌افزاری
مشتری باید **شناسه سخت‌افزاری** رو به شما بده:
- از صفحه لایسنس کپی می‌کنه
- با تلگرام/واتساپ برای شما می‌فرسته

### قدم ۴: نصب کامپوننت‌ها
بعد از فعال‌سازی لایسنس، مشتری می‌تونه:
- Python نصب کنه
- PostgreSQL نصب کنه
- و بقیه موارد...

---

## 4. صدور لایسنس برای مشتری

### 🔐 روش ۱: از طریق پنل ادمین (توصیه شده)

#### قدم ۱: ورود به پنل ادمین
روی سیستم **خودتان** (نه مشتری):
```
http://127.0.0.1:5000/admin_login.html
```
- نام کاربری: `admin`
- رمز عبور: `odoo@2025`

#### قدم ۲: تولید لایسنس
1. شناسه سخت‌افزاری مشتری رو وارد کنید
2. مدت اعتبار رو انتخاب کنید (۱ ماه، ۱ سال، نامحدود...)
3. نام مشتری رو وارد کنید
4. کلیک روی "🔑 تولید کلید لایسنس"

#### قدم ۳: ارسال به مشتری
کلید لایسنس تولید شده رو برای مشتری بفرستید.

### 🔧 روش ۲: از طریق خط فرمان

```batch
python license_manager.py
```

سپس:
1. شناسه سخت‌افزاری وارد کنید
2. مدت اعتبار (ساعت) وارد کنید
3. کلید لایسنس کپی و ارسال کنید

### 📋 روش ۳: دستی با اسکریپت

```batch
generate_license.bat
```

---

## 5. سوالات متداول

### ❓ اگر مشتری کامپیوترش رو عوض کرد چی؟
**پاسخ:** شناسه سخت‌افزاری عوض می‌شه. باید لایسنس جدید صادر کنید.

### ❓ اگر لایسنس منقضی شد؟
**پاسخ:** برنامه کار می‌کنه ولی صفحه لایسنس میاد. باید لایسنس جدید بدید.

### ❓ آیا می‌تونم لایسنس رو لغو کنم؟
**پاسخ:** بله! از پنل ادمین → تاریخچه لایسنس‌ها → دکمه 🚫

### ❓ چطور قیمت‌گذاری کنم؟
**پیشنهاد:**
| مدت | قیمت پیشنهادی |
|-----|--------------|
| ۱ ماه (تست) | رایگان |
| ۱ سال | X تومان |
| نامحدود | Y تومان |

### ❓ اگر اینترنت نباشه لایسنس کار می‌کنه؟
**پاسخ:** بله! لایسنس آفلاین کار می‌کنه چون بر اساس شناسه سخت‌افزاری و امضای دیجیتال هست.

### ❓ آیا کسی می‌تونه لایسنس رو هک کنه؟
**پاسخ:** با کامپایل Nuitka، کد لایسنس داخل EXE محافظت شده و هک کردنش خیلی سخته.

### ❓ چطور مشتریان رو مدیریت کنم؟
**پاسخ:** از پنل ادمین → تب "👥 مشتریان":
- افزودن مشتری جدید
- ویرایش اطلاعات
- مشاهده لایسنس‌های هر مشتری

---

## 📞 فلوچارت کامل

```
┌─────────────────┐
│   شما (ادمین)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ BUILD_RELEASE   │  ← ساخت نسخه محافظت‌شده
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ پوشه release    │  ← انتقال به مشتری
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ سیستم مشتری    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ OdooInstaller   │  ← اجرا
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ صفحه لایسنس    │  ← شناسه سخت‌افزاری نشون میده
└────────┬────────┘
         │
    مشتری شناسه رو
    برای شما می‌فرسته
         │
         ▼
┌─────────────────┐
│ پنل ادمین شما  │  ← تولید لایسنس
└────────┬────────┘
         │
    شما کلید رو
    برای مشتری می‌فرستید
         │
         ▼
┌─────────────────┐
│ مشتری فعال‌سازی │  ← وارد کردن کلید
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ✅ نصب اودو    │  ← استفاده از برنامه
└─────────────────┘
```

---

## 🔧 تنظیمات قبل از ساخت

قبل از ساخت نسخه برای مشتری، این فایل‌ها رو چک کنید:

### admin_config.py
```python
ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "رمز_قوی_خودتان"  # ← تغییر بدید!
```

### license_manager.py
```python
SECRET_KEY = b"کلید_مخفی_خودتان"  # ← تغییر بدید!
```

⚠️ **مهم:** بعد از تغییر، دوباره `BUILD_RELEASE.bat` اجرا کنید.

---

## 📱 ارتباط با مشتری

### پیام نمونه برای درخواست شناسه:
```
سلام! 👋
لطفاً برنامه OdooInstaller.bat رو اجرا کنید.
در صفحه لایسنس، "شناسه سخت‌افزاری" نمایش داده می‌شه.
اون رو برام بفرستید تا کلید لایسنس براتون بسازم.
```

### پیام نمونه برای ارسال لایسنس:
```
سلام! ✅
کلید لایسنس شما:
━━━━━━━━━━━━━━━━━━━━
[کلید لایسنس اینجا]
━━━━━━━━━━━━━━━━━━━━
این کلید رو در صفحه لایسنس وارد کنید و دکمه فعال‌سازی بزنید.
مدت اعتبار: ۱ سال
```
