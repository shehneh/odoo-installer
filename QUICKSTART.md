# 🚀 راهنمای سریع استفاده از داشبورد جدید

## ✅ سرورها در حال اجرا هستند!

### 1️⃣ سرور Frontend (Website)
```
🌐 http://localhost:8080
```

**صفحات موجود:**
- **صفحه خوش‌آمد**: http://localhost:8080/dashboard-welcome.html
- **داشبورد اصلی**: http://localhost:8080/dashboard.html
- **صفحه تست**: http://localhost:8080/dashboard-test.html
- **صفحه اصلی**: http://localhost:8080/index.html

### 2️⃣ سرور Backend (API)
```
🌐 http://localhost:5001
```

**API Endpoints:**
- Health Check: http://localhost:5001/api/health
- User Info: http://localhost:5001/api/user/me
- Demo List: http://localhost:5001/api/demo/list
- Tickets: http://localhost:5001/api/tickets/list

---

## 🎯 چطور تست کنم؟

### روش 1: استفاده از صفحه تست
1. به این آدرس بروید: http://localhost:8080/dashboard-test.html
2. دکمه‌های مختلف را تست کنید:
   - **ساخت دمو**: دیالوگ ساخت دمو را باز می‌کند
   - **اضافه کردن Instance**: یک دمو تستی اضافه می‌کند
   - **تیکت جدید**: فرم ثبت تیکت را باز می‌کند
   - **Toast**: پیام‌های موفقیت/خطا را نشان می‌دهد

### روش 2: استفاده از داشبورد اصلی
1. به این آدرس بروید: http://localhost:8080/dashboard.html
2. داشبورد کامل با تمام ویژگی‌ها را ببینید
3. دکمه "ساخت دمو آنلاین رایگان" را بزنید
4. فرم را پر کنید و دمو بسازید

### روش 3: تست مستقیم API
با Postman یا curl:

```bash
# Health Check
curl http://localhost:5001/api/health

# Get User Info
curl http://localhost:5001/api/user/me

# Create Demo
curl -X POST http://localhost:5001/api/demo/create \
  -H "Content-Type: application/json" \
  -d '{"name": "دموی تستی", "database": "my_demo", "lang": "fa_IR"}'

# Create Ticket
curl -X POST http://localhost:5001/api/tickets/create \
  -H "Content-Type: application/json" \
  -d '{"subject": "تست", "category": "technical", "message": "پیام تست"}'
```

---

## 🎨 ویژگی‌های پیاده شده

### ✅ دموی آنلاین
- [x] ساخت دمو ۱۴ روزه با دیالوگ
- [x] نمایش لیست دموها
- [x] شمارش معکوس زمان باقیمانده
- [x] حذف دمو
- [x] محدودیت 3 دمو همزمان
- [x] باز کردن دمو در تب جدید

### ✅ سیستم تیکت
- [x] ثبت تیکت با دسته‌بندی
- [x] نمایش لیست تیکت‌ها
- [x] وضعیت تیکت (باز/پاسخ داده شده)
- [x] نمایش تعداد پاسخ‌ها
- [ ] پاسخ به تیکت (API آماده، UI در حال توسعه)

### ✅ UI/UX
- [x] Glass Morphism Design
- [x] انیمیشن‌های جذاب
- [x] Hover Effects
- [x] Toast Notifications
- [x] Loading States
- [x] Responsive Design
- [x] RTL Support
- [x] اعداد فارسی

### ✅ کارت‌های آماری
- [x] تعداد لایسنس فعال
- [x] تعداد دانلودها
- [x] روزهای باقیمانده
- [x] تیکت‌های باز

### ✅ Gamification
- [x] سیستم سطح (Level)
- [x] سیستم XP
- [x] دستاوردها (Achievements)
- [x] Progress Bar

---

## 📁 ساختار پروژه

```
Setup odoo19/
├── website/                      # Frontend
│   ├── dashboard.html           # داشبورد اصلی
│   ├── dashboard-test.html      # صفحه تست
│   ├── dashboard-welcome.html   # صفحه خوش‌آمد
│   ├── css/
│   │   ├── dashboard.css        # استایل‌های اصلی
│   │   └── dashboard-additions.css  # استایل‌های جدید
│   └── js/
│       └── demo-manager.js      # مدیریت دمو و تیکت
│
├── api_server.py                # Backend API
├── website_server.py            # Frontend Server
└── data/                        # دیتابیس JSON
    ├── demos.json
    ├── tickets.json
    └── users.json
```

---

## 🔧 نصب و راه‌اندازی

### نیازمندی‌ها
```bash
pip install flask flask-cors
```

### راه‌اندازی
```bash
# Terminal 1: Frontend Server
cd "d:\business\odoo\Setup odoo19"
python website_server.py

# Terminal 2: Backend API
cd "d:\business\odoo\Setup odoo19"
python api_server.py
```

---

## 🚀 مراحل بعدی (در حال توسعه)

### Phase 2: Backend Integration
- [ ] اتصال واقعی به سرور Odoo
- [ ] ساخت Container برای هر دمو
- [ ] مدیریت دیتابیس PostgreSQL
- [ ] Backup و Restore

### Phase 3: Financial
- [ ] سیستم کیف پول
- [ ] فاکتورها
- [ ] تراکنش‌ها
- [ ] درگاه پرداخت

### Phase 4: Communication
- [ ] چت آنلاین با پشتیبانی
- [ ] نوتیفیکیشن Push
- [ ] ایمیل notifications
- [ ] SMS alerts

### Phase 5: Advanced Features
- [ ] PWA Support
- [ ] Dark/Light Theme Toggle
- [ ] Multi-language
- [ ] Analytics Dashboard
- [ ] Export Data

---

## 🐛 عیب‌یابی

### مشکل: صفحه باز نمی‌شود
```bash
# بررسی کنید سرور در حال اجراست
# Terminal را چک کنید و ببینید پورت 8080 free است
```

### مشکل: API کار نمی‌کند
```bash
# بررسی کنید API server روی پورت 5001 در حال اجراست
curl http://localhost:5001/api/health
```

### مشکل: دمو ساخته نمی‌شود
```bash
# لاگ‌های console browser را چک کنید (F12)
# لاگ‌های terminal API server را بررسی کنید
```

---

## 📞 پشتیبانی

برای گزارش باگ یا درخواست ویژگی:
1. لاگ‌های console را کپی کنید
2. اسکرین‌شات بگیرید
3. Issue باز کنید یا تیکت ثبت کنید

---

## 🎉 موفق باشید!

داشبورد جدید آماده استفاده است. از تجربه استفاده لذت ببرید! 🚀

**ساخته شده با ❤️ برای جامعه Odoo ایران**
