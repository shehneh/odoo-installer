# تغییرات نسخه جدید

## نسخه 2.2 - اصلاحات مهم

### 🚀 بهبود فایل start_ui.bat

فایل bat حالا به صورت خودکار:
1. **چک می‌کند** که Python نصب است یا نه
2. اگر Python **نصب نیست**، از پوشه `offline/python` نصب می‌کند
3. منتظر می‌ماند تا نصب کامل شود
4. سپس سرور را اجرا می‌کند

ویژگی‌های جدید:
- چک کردن چندین مسیر رایج Python (3.10 تا 3.13)
- نصب خودکار با درخواست UAC
- تأیید موفقیت نصب قبل از ادامه
- پیام‌های واضح در هر مرحله

### 💻 تشخیص خودکار معماری سیستم

سیستم حالا تشخیص می‌دهد:
- **نسخه ویندوز** (Windows 10/11)
- **معماری پردازنده** (x64 یا x86)
- **نسخه سیستم‌عامل** و edition

بر اساس این اطلاعات:
- نرم‌افزار **سازگار** را انتخاب می‌کند
- نرم‌افزارهای **ناسازگار** را غیرفعال می‌کند
- در صورت نیاز **دانلود خودکار** انجام می‌دهد

### 📥 دانلود و نصب خودکار

اگر نرم‌افزار متناسب با سیستم شما در پوشه `soft/` نباشد:
1. دکمه "📥 دانلود و نصب" ظاهر می‌شود
2. کلیک کنید - دانلود شروع می‌شود
3. به صورت خودکار نصب می‌شود

نرم‌افزارهای پشتیبانی شده:
- VSCode (x64/x86)
- Python (x64/x86)
- WinRAR (x64/x86)
- Git (x64/x86)

---

## نسخه 2.1 - بهبودهای عمده

### 🔧 رفع مشکلات اصلی

1. **نصب واقعی (Real Installation)**
   - قبلاً نصب فقط شبیه‌سازی می‌شد و واقعاً فایلی نصب نمی‌شد
   - حالا سیستم منتظر می‌ماند تا هر برنامه به طور کامل نصب شود
   - از سیستم Polling هر 3 ثانیه برای تأیید نصب استفاده می‌شود
   - زمان انتظار برای هر نرم‌افزار مشخص است:
     * Python: 90 ثانیه
     * PostgreSQL: 120 ثانیه
     * VC++ Redistributable: 30 ثانیه
     * wkhtmltopdf: 30 ثانیه
     * Python Packages: 60 ثانیه
     * Database Role: 15 ثانیه

2. **اولویت نصب Python**
   - Python حتماً **اول از همه** نصب می‌شود
   - اگر نصب Python با خطا مواجه شود، فرآیند متوقف می‌شود
   - بدون Python نصب شده، bat file خطا می‌دهد - این مشکل حل شد

3. **ترتیب اولویت نصب**
   ```
   1. Python (اولویت اول - الزامی)
   2. VC++ Redistributable
   3. PostgreSQL
   4. wkhtmltopdf
   5. Python Packages (pip wheels)
   6. Database Role Creation
   ```

### ✨ ویژگی‌های جدید

1. **بخش ابزارهای توسعه‌دهنده (Developer Tools)**
   - نرم‌افزارهای موجود در پوشه `soft/` به UI اضافه شدند
   - می‌توانید مستقیماً از رابط کاربری نصب کنید:
     * WinRAR
     * speedtop.exe
     * VSCode
     * copilot-rtl (افزونه VS Code)
   - هر ابزار با اندازه فایل نمایش داده می‌شود
   - نصب آفلاین با یک کلیک

2. **API جدید**
   - `/api/check_install_status` - برای polling وضعیت نصب
   - `/api/install/install_soft_tool?path=...` - برای نصب ابزارهای توسعه

3. **پیام‌های بهتر**
   - نمایش درصد پیشرفت واقعی در حین نصب
   - Toast notifications برای هر مرحله
   - اطلاع‌رسانی موفقیت یا خطای هر نصب

### 🎯 بهبودهای UX

- دکمه "نصب کامل" حالا اخطار واضح‌تری نمایش می‌دهد
- نوار پیشرفت به طور دقیق بروز می‌شود
- منتظر ماندن واقعی تا تکمیل هر مرحله
- نمایش زمان سپری شده برای هر نصب

### 📝 نکات مهم

- **همیشه Python را اول نصب کنید** - این الزامی است
- اگر نصب Python ناموفق بود، دوباره تلاش کنید
- نصب PostgreSQL ممکن است تا 2 دقیقه طول بکشد
- دسترسی Administrator برای همه نصب‌ها الزامی است

### 🔍 فایل‌های تغییر یافته

- `ui_server.py` - اضافه شدن API جدید و پشتیبانی از soft tools
- `web/app.js` - بازنویسی کامل سیستم نصب با polling واقعی
- `web/index.html` - اضافه شدن بخش Developer Tools

---

**تاریخ:** 24 دسامبر 2025
**نسخه:** 2.1.0
