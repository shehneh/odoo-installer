# 🔐 راهنمای سیستم لایسنس

## نصب و راه‌اندازی

سیستم لایسنس برای حفاظت از نرم‌افزار Odoo Offline Installer پیاده‌سازی شده است.

## ویژگی‌های سیستم لایسنس

✅ **رمزنگاری امن**: کلید لایسنس با HMAC-SHA256 رمزنگاری می‌شود
✅ **وابسته به سخت‌افزار**: هر کلید فقط برای یک دستگاه معتبر است  
✅ **تاریخ انقضا**: امکان تعیین تاریخ انقضا برای لایسنس
✅ **فایل قفل رمزشده**: لایسنس در فایل `.license` رمزشده ذخیره می‌شود
✅ **بررسی خودکار**: در هر بار اجرا، لایسنس بررسی می‌شود

## نحوه استفاده

### 1️⃣ تولید کلید لایسنس (برای توسعه‌دهنده)

برای تولید کلید لایسنس برای یک دستگاه:

```bash
# روش 1: اجرای مستقیم
python license_manager.py

# روش 2: استفاده از فایل bat
generate_license.bat
```

این دستور موارد زیر را نمایش می‌دهد:
- **Hardware ID** دستگاه
- کلیدهای لایسنس با اعتبار مختلف (30 روز، 90 روز، 1 سال، 10 سال)

### 2️⃣ فعال‌سازی لایسنس (برای کاربر)

1. نرم‌افزار را اجرا کنید (`start_ui.bat` یا `python ui_server.py`)
2. مرورگر به صفحه فعال‌سازی هدایت می‌شود
3. **Hardware ID** دستگاه خود را ببینید
4. این شناسه را برای توسعه‌دهنده ارسال کنید
5. کلید لایسنس دریافتی را وارد کنید
6. روی "فعال‌سازی" کلیک کنید

### 3️⃣ مدیریت لایسنس

#### بررسی وضعیت لایسنس
```python
from license_manager import get_license_info

info = get_license_info()
print(info)
# Output: {'is_valid': True, 'message': 'لایسنس معتبر است...', ...}
```

#### غیرفعال‌سازی لایسنس (برای انتقال به دستگاه دیگر)
```python
from license_manager import deactivate_license

deactivate_license()
```

یا به صورت دستی فایل `.license` را حذف کنید.

## ساختار فایل‌ها

```
Setup odoo19/
├── license_manager.py          # ماژول مدیریت لایسنس
├── generate_license.bat        # تولید کلید (Windows)
├── .license                    # فایل لایسنس رمزشده (خودکار ایجاد می‌شود)
├── ui_server.py               # سرور اصلی (با چک لایسنس)
└── web/
    ├── license.html           # صفحه فعال‌سازی لایسنس
    ├── index.html            # صفحه اصلی (نیاز به لایسنس معتبر)
    └── app.js                # کد JavaScript (با چک لایسنس)
```

## API Endpoints

### GET `/api/license/status`
بررسی وضعیت لایسنس

**Response:**
```json
{
  "licensed": true,
  "message": "لایسنس معتبر است (باقیمانده: 365 روز)",
  "hardware_id": "8e9666bd4cdbe30d",
  "expiry_date": "2026-12-25",
  "days_remaining": 365
}
```

### POST `/api/license/activate`
فعال‌سازی کلید لایسنس جدید

**Request:**
```json
{
  "license_key": "OGU5NjY2YmQ0Y2RiZTMwZDoyMDI2LTEyLTI1OmM0OTE2MzNiNGNlYjcyYTI="
}
```

**Response:**
```json
{
  "success": true,
  "message": "لایسنس معتبر است (باقیمانده: 365 روز)"
}
```

### POST `/api/license/deactivate`
غیرفعال‌سازی لایسنس فعلی

**Response:**
```json
{
  "success": true,
  "message": "لایسنس با موفقیت غیرفعال شد"
}
```

## امنیت

### تغییر کلید مخفی (SECRET_KEY)

⚠️ **مهم**: قبل از توزیع نرم‌افزار، حتماً `SECRET_KEY` را تغییر دهید!

در فایل `license_manager.py`:

```python
SECRET_KEY = b'OdooInstallerSecretKey2025'  # این را به یک مقدار تصادفی تغییر دهید
```

برای تولید کلید مخفی امن:

```python
import secrets
print(secrets.token_bytes(32))
# مقدار خروجی را در SECRET_KEY قرار دهید
```

### نکات امنیتی

1. ✅ کلید مخفی (`SECRET_KEY`) را **هرگز** در گیت قرار ندهید
2. ✅ فایل `.license` را در `.gitignore` اضافه کنید
3. ✅ از HTTPS برای ارسال کلید لایسنس استفاده کنید
4. ✅ کلیدهای لایسنس تولیدی را در دیتابیس امن ذخیره کنید
5. ✅ برای محیط production، کد bypass را حذف کنید

## سفارشی‌سازی

### تغییر مدت اعتبار پیش‌فرض

در `license_manager.py`:

```python
def generate_license_key(hardware_id: str, expiry_days: int = 365):
    # expiry_days را به دلخواه تغییر دهید
```

### غیرفعال کردن سیستم لایسنس (برای توسعه)

در `ui_server.py`:

```python
LICENSE_ENABLED = False  # برای bypass کامل
```

یا فایل `license_manager.py` را rename/حذف کنید.

### اضافه کردن فیلدهای اضافی به لایسنس

می‌توانید اطلاعات بیشتری مثل نام شرکت، ایمیل، تعداد کاربر مجاز و... را به لایسنس اضافه کنید:

```python
def generate_license_key(hardware_id: str, expiry_days: int = 365, 
                        company_name: str = '', max_users: int = 1):
    expiry_date = (datetime.now() + timedelta(days=expiry_days)).strftime('%Y-%m-%d')
    data = f"{hardware_id}:{expiry_date}:{company_name}:{max_users}"
    # ...
```

## عیب‌یابی

### خطا: "لایسنس یافت نشد"
- فایل `.license` وجود ندارد
- حل: کلید لایسنس را مجدداً وارد کنید

### خطا: "این لایسنس برای این دستگاه معتبر نیست"
- سخت‌افزار تغییر کرده (مثل تعویض مادربورد)
- حل: از توسعه‌دهنده کلید جدید برای Hardware ID جدید بگیرید

### خطا: "لایسنس منقضی شده است"
- تاریخ انقضا گذشته
- حل: از توسعه‌دهنده لایسنس جدید بگیرید

### خطا: "لایسنس تقلبی است"
- کلید دستکاری شده یا `SECRET_KEY` تغییر کرده
- حل: از کلید اصلی استفاده کنید

## پشتیبانی

برای سوالات یا مشکلات:
- ایمیل: your-email@example.com
- تلگرام: @your_telegram

---

**© 2025 - سیستم مدیریت لایسنس Odoo Installer**
