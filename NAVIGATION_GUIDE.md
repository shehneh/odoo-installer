# 🧭 سیستم Navigation یکپارچه OdooMaster

## ✅ مشکل حل شده

قبلاً مشکل اصلی این بود که:
- ❌ از صفحه اصلی نمی‌شد به Odoo دسترسی پیدا کرد
- ❌ لینک‌های واضح و منسجمی وجود نداشت
- ❌ نمی‌شد راحت بین صفحات جابجا شد

## 🎯 راه‌حل: منوی یکپارچه

یک **navigation bar یکپارچه** در بالای تمام صفحات اضافه شد که شامل:

### 📋 دسته‌بندی منو:

#### 1️⃣ منوی اصلی
- 🏠 **صفحه اصلی** (`index.html`)
- 👁️ **دمو رایگان** (`demo.html`) - مشاهده نصاب بدون نیاز به نصب
- 🚀 **نصب آنلاین** (`install.html`) - نصاب واقعی Odoo
- 📊 **داشبورد من** (`dashboard.html`) - پنل کاربری شما

#### 2️⃣ Odoo
- ➕ **ثبت‌نام تِنَنت** (`register_tenant.html`) - ساخت Odoo اختصاصی
- 📥 **دانلودها** (`downloads.html`) - فایل‌های نصب
- 🎧 **پشتیبانی** (`support.html`) - تماس با پشتیبانی

#### 3️⃣ اطلاعات
- 📖 **مستندات** (`docs.html`) - راهنمای کامل
- 🔑 **لایسنس‌ها** (`licenses.html`) - مدیریت لایسنس
- 💳 **قیمت‌گذاری** (`payment.html`) - خرید و پرداخت

---

## 🎨 ویژگی‌های طراحی

### 1. منوی ثابت بالای صفحه
- همیشه در بالا قابل مشاهده است
- پس‌زمینه شفاف با افکت blur
- هایلایت صفحه فعلی

### 2. Breadcrumb Navigation
- نمایش مسیر فعلی شما
- لینک سریع به خانه

### 3. دکمه شناور Quick Actions
- **موقعیت:** پایین سمت چپ صفحه
- **دکمه آبی شناور** با آیکون قطب‌نما 🧭
- با کلیک روی آن، دسترسی سریع به:
  - 🚀 نصب آنلاین
  - ▶️ دمو رایگان
  - 📊 داشبورد من
  - 🏠 صفحه اصلی

---

## 📱 Responsive Design

### Desktop
- منو کامل در یک خط افقی
- دسته‌بندی واضح

### Mobile/Tablet
- دکمه همبرگر منو (☰)
- منو کشویی از بالا
- قابلیت اسکرول

---

## 🔗 نحوه استفاده

### از هر صفحه‌ای:

1. **برای نصب Odoo:**
   - کلیک روی "نصب آنلاین" 🚀 (highlight شده)
   - یا از دکمه شناور پایین چپ

2. **برای مشاهده دمو:**
   - کلیک روی "دمو رایگان" 👁️

3. **برای ورود به داشبورد:**
   - کلیک روی "داشبورد من" 📊

4. **برای برگشت به خانه:**
   - کلیک روی لوگو OdooMaster
   - یا "صفحه اصلی" 🏠

---

## 🎯 مثال سناریوهای استفاده

### سناریو 1: کاربر جدید می‌خواهد Odoo نصب کند
```
صفحه اصلی → نصب آنلاین → [فرم نصب]
```

### سناریو 2: مشاهده دمو قبل از خرید
```
صفحه اصلی → دمو رایگان → [پیش‌نمایش نصاب]
```

### سناریو 3: مدیریت Odoo های موجود
```
صفحه اصلی → داشبورد من → [لیست تننت‌ها]
```

### سناریو 4: ساخت تننت جدید از داشبورد
```
داشبورد → ثبت‌نام تِنَنت → [فرم ساخت Odoo]
```

---

## 💻 فایل‌های اضافه شده

```
website/
├── css/
│   └── unified-nav.css      ← استایل منوی یکپارچه
└── js/
    └── unified-nav.js       ← منطق navigation
```

### فایل‌های ویرایش شده:
- ✅ `index.html`
- ✅ `install.html`
- ✅ `demo.html`
- ✅ (سایر صفحات به زودی)

---

## 🚀 نتیجه

✅ **Navigation روشن و واضح**  
✅ **دسترسی سریع به تمام بخش‌ها**  
✅ **تجربه کاربری یکپارچه**  
✅ **طراحی مدرن و حرفه‌ای**

---

## 📝 توجه برای توسعه‌دهندگان

برای اضافه کردن منو به صفحات جدید:

```html
<!-- در <head> -->
<link rel="stylesheet" href="css/unified-nav.css">

<!-- قبل از </body> -->
<script src="js/unified-nav.js"></script>
```

این منو به صورت خودکار:
- در بالای صفحه قرار می‌گیرد
- صفحه فعلی را highlight می‌کند
- Breadcrumb اضافه می‌کند
- دکمه Quick Actions نمایش می‌دهد
